import { BaseContent } from "@/components/isomorphic";

export default function HomePage() {
  return (
    <BaseContent
      // breadcrumb={(
      //   "breadcrumb"
      // )}
      isEmpty
    >
      ¡Bienvenido!
    </BaseContent>
  );
}
