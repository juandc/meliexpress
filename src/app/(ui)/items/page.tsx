import { BaseContent } from "@/components/isomorphic";

export default function SearchPage() {
  return (
    <BaseContent
      breadcrumb={(
        "breadcrumb"
      )}
    >
      Search items...
    </BaseContent>
  );
}
