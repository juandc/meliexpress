import { BaseContent } from "@/components/isomorphic";

export default function ItemPage() {
  return (
    <BaseContent
      breadcrumb={(
        "breadcrumb"
      )}
    >
      Item result...
    </BaseContent>
  );
}
